export const metadata = {
  title: "Projects | Aluminox",
  description:
    "Explore Aluminox facade engineering and exterior envelope projects delivered across commercial and residential sectors in the UAE.",
};

export default function ProjectsLayout({ children }) {
  return children;
}
